#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point.
 *
 * Description: A C program that prints exactly \"Programming
 * is like building multilingual puzzle
 * followed by new line.
 * Using only the puts() function,
 * You are not allowed to use printf() function.
 * Your program should end with the value 0.
 *
 * Return: Always 0 (Success).
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");

	return (0);
}
